name: CI Develop

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
  workflow_dispatch:

jobs:
  Build:
    name: Build Test (x86_64)
    runs-on: ubuntu-latest
    container:
      image: vladkosten/embedded_env:latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -B build -DMATRIX_KBD_BUILD_TESTS=ON
      - name: Build
        run: cmake --build build
      - name: Upload test binary
        uses: actions/upload-artifact@v4
        with:
          name: test-binary
          path: ./build/test/test_runner/MatrixKbdUnitTestRun

  CppCheck:
    name: CppCheck
    needs: Build
    runs-on: ubuntu-latest
    container:
      image: vladkosten/embedded_env:latest
    steps:
      - uses: actions/checkout@v4
      - name: Run CppCheck
        run: |
          mkdir -p reports
          cppcheck --enable=warning,performance,portability \
                   --inconclusive \
                   --language=c \
                   --std=c11 \
                   --template="[{severity}][{id}] {message} {callstack}" \
                   --suppress=ConfigurationNotChecked \
                   --suppress=unreachableCode \
                   --error-exitcode=1 \
                   --inline-suppr \
                   --output-file=reports/cppcheck.txt \
                   main/ portable/
          cat reports/cppcheck.txt
      - name: Upload CppCheck report
        uses: actions/upload-artifact@v4
        with:
          name: cppcheck-report
          path: ./reports/cppcheck.txt

  Misra:
    name: MISRA
    needs: Build
    runs-on: ubuntu-latest
    container:
      image: vladkosten/embedded_env:latest
    steps:
      - uses: actions/checkout@v4
      - name: Run MISRA
        run: |
          mkdir -p reports
          cppcheck --enable=style \
                   --inconclusive \
                   --language=c \
                   --std=c11 \
                   --addon=misra.py \
                   --template="[{severity}][{id}] {message} {callstack}" \
                   --suppress=variableScope \
                   --suppress=unreadVariable \
                   --suppress=unreachableCode \
                   --output-file=reports/misra.txt \
                   main/ portable/
          cat reports/misra.txt
      - name: Upload MISRA report
        uses: actions/upload-artifact@v4
        with:
          name: misra-report
          path: ./reports/misra.txt

  ClangFormat:
    name: Clang Format
    needs: Build
    runs-on: ubuntu-latest
    container:
      image: vladkosten/embedded_env:latest
    steps:
      - uses: actions/checkout@v4
      - name: Run clang-format
        run: |
          find main/ portable/ -name '*.c' -o -name '*.h' | \
            grep -v 'mocks\.' | \
            xargs clang-format --dry-run --Werror

  Test:
    name: Unit Test
    needs: Build
    runs-on: ubuntu-latest
    steps:
      - name: Download test binary
        uses: actions/download-artifact@v4
        with:
          name: test-binary
          path: ./
      - name: Run tests
        run: |
          chmod +x ./MatrixKbdUnitTestRun
          ./MatrixKbdUnitTestRun -v

  Metrics:
    name: Metrics
    needs: Build
    runs-on: ubuntu-latest
    container:
      image: vladkosten/embedded_env:latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Lizard
        run: |
          pip install lizard
          mkdir -p metrics
          lizard main/ portable/ --xml > metrics/lizard.xml
          lizard main/ portable/ --csv > metrics/lizard.csv
          echo "=== Summary ==="
          lizard main/ portable/ --warnings 0.5 20 4 15 5
      - name: Upload metrics
        uses: actions/upload-artifact@v4
        with:
          name: metrics
          path: ./metrics/
